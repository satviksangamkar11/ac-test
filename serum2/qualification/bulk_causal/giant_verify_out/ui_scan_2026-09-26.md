# UI scan of VERIFY_REFERENCE_FULL (2026-09-26, Serum 2 VST3 sha256 9293eb90..., loaded via create_serum_track)

## lfo1..6.mode -- ROOT CAUSE FOUND (cross-parameter constraint, not a write bug)
- With shape = S&H (target `lfo*.shape = RandomSH`), Serum greys out the ENVELOPE mode button; clicking it does nothing.
- Live test on the loaded instance: switched LFO1 shape S&H -> Normal only. ENVELOPE became clickable and lit when clicked.
- So `mode = Envelope` is unavailable while `shape = S&H`, and Serum drops it. The isolation tests passed because their
  shape was the default (Normal). The giant preset combines both targets. No bisect needed.
- Classification: `lfo*.mode` target is incompatible with the co-applied `lfo*.shape` target -> CONTEXT_CONFLICT, not MISMATCH.
- LFO2-6 show the same state as LFO1 (S&H, ENVELOPE greyed, 500.0 Hz, MONO on, TRIP/DOT on).

## osc*.warp_mode / warp_amount / warp_var2 -- NOT OBSERVABLE in this preset
- Oscillators are in SAMPLE mode (structural OSC_SAMPLE merge). The warp panel shows `SampleOsc1` warp
  (`kFM_OSC` -> "FM (A)", Thru-Zero; slot 2 Off), not `WTOsc1` where the targets were written.
- File bytes: `WTOsc1 = {kParamWarp: 0.5, kParamWarpMenu: 'kAM_OSC', kParamWarpMenu2: 'fm', kParamWarpVar2: 0.5}`,
  `SampleOsc1 = {kParamWarp: 0.0, kParamWarpMenu: 'kFM_OSC'}`.
- Side issue: `WTOsc1.kParamWarpMenu2` holds the spec word `'fm'`, not a raw value like `kFM_OSC` (the manifest's leaf_set
  table maps fm -> kFM_OSC). Looks like the giant builder wrote the word into the raw slot. Needs a wavetable-mode preset to verify.

## fx.compressor.* -- EARLIER FINDING WAS A COLUMN MISREAD (corrects DIRECT_UI_FINDING_RAW_WRITE_GAP.md section 2 & 3)
Header row reads `-7.5 dB | Limit | 1.0 | 100.0 | 30.4` over THRESH / RATIO / ATTACK / RELEASE / GAIN. Each knob was
hover-confirmed (tooltip "FX 3: Comp <name> (<value>)"):

| control | file raw value | Serum shows | verdict |
|---|---|---|---|
| thresh  | 0.25       | -7.5 dB   | curve unknown (consistent with display = -30 dB x raw, unverified) |
| ratio   | 31622.78   | Limit     | knob at top of range; schema max 1e6 is wider than Serum's range -> not a numeric match |
| attack  | 100.0      | 1.0 ms    | MISMATCH (earlier doc called this a MATCH -- it had read the RELEASE column) |
| release | 100.0      | 100.0 ms  | MATCH (earlier doc called this a MISMATCH -- it had read the GAIN column) |
| gain    | 16.5       | 30.4 dB   | MISMATCH, curve unknown |

attack and release hold the same raw number with the same declared domain (log 0.1-1000) but display differently, so
the two slots do not share one encoding. The earlier "RATIO 1.0 / RELEASE 30.4" readings were ATTACK and GAIN.
The isolated test readings ("RATIO 0.9, RELEASE 0.0") were likely the same column shift and should be re-read.

## fx.reverb.type -- VOCABULARY MISMATCH
- File: `kParamType = 'kAbyss'`. Serum shows NITROUS. Serum 2.0.23's reverb menu is Plate / Hall / Vintage / Nitrous / Basin.
- serum-mcp's schema vocabulary is `kAbyss, kHall, kSpace, kVintage`: `kAbyss` and `kSpace` are not Serum names, and
  Plate / Nitrous / Basin are missing. Unknown whether Nitrous is Serum's alias for `kAbyss` or a fallback; either
  way the schema vocabulary does not match this Serum version.

## FX page, units not reached by the earlier scan (hover-confirmed unless noted)
- FX Filter: type Low 6 MATCH, reso 50% MATCH, drive 50% MATCH, cutoff raw 0.25 -> 59 Hz (curve unverified)
- Flanger: rate 5.00 Hz, depth 50%, feedback 25% MATCH
- Phaser: rate 10.00 Hz, depth 25%, freq 3557 Hz, feedback 50% MATCH
- Reverb: size 50% MATCH; type see above
- Hyper: wet 25% MATCH; unison raw 3.5 shows 4 (integer display, NORMALIZED)
- Distortion: freq raw 0.25 -> 52 Hz (curve unverified), Q 1.9
- Dimension size / mix: no hover tooltip; knob positions look like ~25% / ~50% (visual estimate only)
- Wet/mix knobs: tooltips did not render in the zoom window; not individually confirmed on this pass

## MATRIX page
All rows empty. The plan has no matrix targets, so there is nothing to compare.

## ARP page (read directly, no tooltips needed)
- pattern shape Chord, rate 1/4 (0.25), transpose shift -12, range 6, gate 50, chance 50: MATCH
- thru on, retrigger launch off, note on, first on, velocity enable on, velocity retrig on: MATCH
- velocity target knob: no tooltip; the knob sits left of centre, so 50 is not confirmed (visual only)
- `arp.transpose.shape` (NOT_DERIVED, vocabulary unknown) shows **Up** in the reference. Its full menu, in Serum's order:
  Up, Down, Up/Down, Down/Up, Up+Down, Down+Up, Thumb Up, Thumb UD, Pinky Up, Pinky UD, Converge, Diverge,
  Con+Diverge, Chord, Random, Rand No Dup, Rand Drift, Rand Once (18 items). The display labels are known; the raw
  stored strings still need one file write per label to learn.
- Same list vs the schema's `arp.pattern.shape` domain (15 values): the schema has no Up, Pinky Up or Pinky UD. If
  pattern shape uses the same list, those three are missing from serum-mcp.
- The live instance's transpose shape was stepped to Thumb Up while enumerating; the file is unchanged.

## Session 2 findings (2026-09-26, three focused checks)

### LFO context check — CONFIRMED (VERIFY_REFERENCE_FULL reloaded clean)
- Preset reloaded fresh from disk (no asterisk in title bar).
- LFO1 panel: Shape = S&H, Mode = FREE (lit), ENVELOPE greyed out (→ icon, not a selectable button).
- This matches the CONTEXT_CONFLICT finding in CORRECTION 2 of DIRECT_UI_FINDING_RAW_WRITE_GAP.md.
- No new evidence needed; the constraint is reproduced on reload and documented.

### Wavetable warp scan — VERIFY_SECONDARY_OSC_WARP2 (hover-confirmed)
Preset loaded. All three OSCs show WAVETABLE mode in the header. Warp controls are in the WTOsc section (not SampleOsc).

File bytes (read via unpack_file): for OSC A/B/C (Oscillator0-2) identically:
  kParamWarp=0.0, kParamWarpMenu='kFM_OSC', kParamWarp2=0.5, kParamWarpMenu2='kFM_OSC'

GUI hover readings:

| OSC | warp_amount | warp_mode | warp_mode2 | warp_var2 |
|-----|------------|-----------|------------|-----------|
| A   | 0 %        | FM (B)    | FM (B)     | 50 %      |
| B   | 0 %        | FM (A)    | FM (A)     | 50 %      |
| C   | 0 %        | FM (A)    | FM (A)     | 50 %      |

Mapping confirmed: `kFM_OSC` is contextual — for OSC A it displays "FM (B)" (modulated by B),
for OSC B/C it displays "FM (A)" (modulated by A). All warp_amount and warp_var2 values match targets (0.0 → 0%, 0.5 → 50%).

Side issue resolved: the VERIFY_REFERENCE_FULL preset wrote the spec word `'fm'` into kParamWarpMenu2 instead
of the enum value `'kFM_OSC'`. The correct raw value is `'kFM_OSC'` as confirmed by the secondary preset's
file bytes and the resulting GUI display. Whether Serum silently defaults or displays an error when it reads
`'fm'` remains unverified (oscA/B/C are in SAMPLE mode in VERIFY_REFERENCE_FULL, hiding WTOsc controls).

warp_mode controls ARE visible and readable in WavetableMode — the 12 previously unobservable warp controls
can now be evaluated via a WT-mode preset. The display labels for the full FM vocabulary need a separate
click-through scan if the remaining enum values (kAM_OSC, kHardSync, etc.) are needed.

### Compressor — already documented in CORRECTION 2 (no new read needed)
The five compressor controls were already hover-confirmed in the earlier session against VERIFY_REFERENCE_FULL:
  Thresh = -7.5 dB, Ratio = Limit, Attack = 1.0 ms, Release = 100.0 ms, Gain = 30.4 dB
The earlier column misread (attack↔gain transposition) is fully corrected in DIRECT_UI_FINDING_RAW_WRITE_GAP.md
CORRECTION 2. The ISOLATE_FXCOMP_RATIO preset was located (giant_verify_out/) but not loaded this session
because the VERIFY_REFERENCE_FULL compressor hover evidence is already sufficient.

## BULK_01_WAVETABLE_MAIN session (2026-09-26, clean reload confirmed, no asterisk)

**Tier note:** B_host_text (DawDreamer dump) and C_DIRECT_UI (on-screen reads) are kept separate below.
B_host_text confirms the value landed in Serum's DSP state but is NOT sufficient to close a control on its own.
C_DIRECT_UI sections contain only what was directly observed on screen (zoom, label reads, header rows, mode buttons).

### Host parameter dump (B_host_text tier, DawDreamer, 126 params changed from init)
Generated by `dump_reference_parameter_text.py`. Key confirmed readings:

| control (schema) | raw | host display | notes |
|---|---|---|---|
| oscA.warp_mode | 0.0145 | 'Sync' | kSync → 'Sync' ✓ |
| oscA.warp_mode2 | 0.8406 | 'AM (B)' | kAM_OSC → 'AM (B)' ✓ |
| oscA.warp_amount | 0.46 | '2.46' | SYNC mode: nonlinear (not percent_linear) |
| oscA.warp_amount2 | 0.63 | '63' | AM(B) mode: percent_linear ✓ |
| oscA.warp_var2 | 0.79 | '0.7900' | identity ✓ |
| oscB.warp_mode | 0.0725 | 'PWM' | kPWM → 'PWM' ✓ |
| oscB.warp_mode2 | 0.0145 | 'Sync' | kSync → 'Sync' ✓ |
| oscB.warp_amount | 0.42 | '58' | PWM mode: inverted (1-0.42)*100=58 ✓ |
| oscB.warp_amount2 | 0.32 | '1.49' | SYNC mode: nonlinear (same curve as A Warp) |
| oscC.warp_mode | 0.0290 | 'Bend +' | kBendPos → 'Bend +' ✓ |
| oscC.warp_mode2 | 0.0725 | 'PWM' | kPWM → 'PWM' ✓ |
| oscC.warp_amount | 0.18 | '18' | Bend+ mode: percent_linear ✓ |
| oscC.warp_amount2 | 0.61 | '39' | PWM mode: inverted (1-0.61)*100=39 ✓ |
| env1.sustain | 0.16 | '-31.8 dB' | curve: 40*log10(raw) ✓ |
| env2.sustain | 0.55 | '30%' | curve: raw²*100 = 30.25 ✓ |
| env3.sustain | 0.56 | '31%' | curve: raw²*100 = 31.36 ✓ |
| env4.sustain | 0.45 | '20%' | curve: raw²*100 = 20.25 ✓ |
| filter1.type | 0.0472 | 'Low 12' | schema 'Low 12' → 'Low 12' ✓ |
| filter1.freq | 0.19 | '172' | nonlinear Hz curve |
| filter2.freq | 0.41 | '873' | nonlinear Hz curve |
| lfo1.rate | 0.4729 | '1/2 t' | beat-sync + triplet ✓ |
| lfo1-6.smooth | 0.91-0.96 | '91'-'96' | percent_linear ✓ |
| macro2-8 | 0.73/0.46/0.19/0.63/0.36/0.79 | '73'/'46'/'19'/'63'/'36'/'79' | percent_linear ✓ |
| global.voice_amp | 0 | '0.0000' | default |
| swing | 0.30 | '35.0%' | NOT percent_linear (0.30→35%) |
| Bus 1 Vol | 0.7071 | '71% [6.0 dB]' | raw=1/√2; dB display non-obvious |

LFO 2-6 rate/delay/rise also confirmed matching (beat-sync note labels). Full dump at BULK_01_host_texts.json.

### Warp mode labels (OSC page, direct read via zoom)
- OSC A WARP labels: **SYNC | AM(B)** (kSync→'Sync', kAM_OSC→'AM (B)') ✓
- OSC B WARP labels: **PWM | SYNC** (kPWM→'PWM', kSync→'Sync') ✓
- OSC C WARP labels: **BEND+ | PWM** (kBendPos→'Bend +', kPWM→'PWM') ✓
- Warp indicator bar: position 1=first warp slot, position 2=second slot (visible green fill)

### Warp amount display curves (B_host_text tier, both host text and on-screen tooltip agree)
Warp amount display depends on the active warp mode — each mode has its own curve:

| Warp mode | Display formula | Evidence |
|---|---|---|
| AM (B) / Bend+ | percent_linear: display = raw × 100 | A Warp2=0.63→63, C Warp=0.18→18 ✓ |
| PWM | inverted: display = (1−raw) × 100 | B Warp=0.42→58, C Warp2=0.61→39 ✓ |
| Sync | nonlinear: raw=0.46→2.46, raw=0.32→1.49 | A Warp, B Warp2 ✓ (formula unknown) |

These are C_DIRECT_UI (warp mode labels confirmed on screen) + B_host_text (amounts confirmed in DawDreamer dump).
The raw values land correctly in the file; the display differences are mode-dependent scale factors, not write mismatches.

### FX compressor (header row direct read, BULK_01 clean load)
- THRESH: -17.5 dB (raw=0.49 → model: -41.7*0.49+2.9 = -17.5 ✓)
- RATIO: **Limit** (raw=430.0 — ceiling below 430)
- ATTACK: 0.2 ms (raw=22.0 → 22/100 = 0.22 ≈ 0.2 ✓, confirmed earlier formula)
- RELEASE: 90.1 ms (default, not written)
- GAIN: 26.7 dB (raw=11.0 → linear model: 0.67*11+19.3=26.67 ✓)

### FX reverb type (BULK_01)
- kHall → displays **"Hall"** (BULK_01 direct read from label) ✓
- Reverb parameters visible in header: LO CUT=0, HI CUT=35, SPIN RATE=25, SPIN DEPTH=20 (all defaults)
- Raw values from file: SIZE=76.0, WET=23.0 (hover blocked by accidental-modification risk)

### LFO 1-6 mode (BULK_01, all 6 confirmed by tab clicks — C_DIRECT_UI)
- LFO1-6: **ENVELOPE active (→ icon lit)** for all 6 tabs ✓
- LFO1-6 shape: **Normal** (default shape in BULK_01)
- lfo*.mode = 'Envelope' MATCH for all 6 ✓

### LFO 7 (file LFO6, the conflict reference)
- File: `LFO6 = {kParamMode: 'Envelope', kParamType: 'RandomSH'}` — the S&H + Envelope conflict reference
- **UI tab not found**: Serum 2.0.23's bottom tab bar shows exactly LFO1–LFO6, then VELO, NOTE. No LFO 7 tab visible. The file slot LFO6 (0-indexed) cannot be directly verified by tab click in this build.
- File LFO7-LFO9 also exist (all mode=Free), confirming Serum 2 stores up to 10 LFO slots in the file even when the UI only surfaces 6.

### Sustain display curves (BULK_01, B_host_text confirmed)
- Env1 sustain: 40*log10(raw) dB confirmed (two presets, multiple points)
- Env2-4 sustain: raw²*100 percent confirmed (four calibration points across two presets)
- Earlier 'mismatch' fully resolved: the display curves are NOT linear

### GLOBAL page (BULK_01)
- QUALITY: **Ultra** (oversampling raw=2.0 → 'Ultra') ✓
- Serum build: 2.0.23 vst, date 25-10-08_1b94c91

---

## BULK_02_SAMPLE_CALIBRATION session (2026-09-26, loaded immediately after BULK_01)

### FX compressor (header row, BULK_02)
- THRESH: **-13.3 dB** (raw=0.4, model: -41.7*0.4+2.9 = -13.8 dB; actual -13.3 dB, 0.5 dB residual)
- RATIO: **Limit** (raw=210 — ceiling is BELOW 210; narrowed from below-430 to below-210)
- ATTACK: **0.1 ms** (raw=10.0 → 10/100 = 0.1 ✓)
- RELEASE: 90.1 ms (default)
- GAIN: **23.8 dB** (raw=8.0, model: 0.67*8+19.3 = 24.66 dB; actual 23.8 dB, 0.9 dB off)

Compressor ratio "Limit" ceiling: three data points all show "Limit" (raw=210, 430, 31622). Effective ceiling must be below 210. The serum-mcp schema's ratio domain writes far above Serum's actual display range.

Compressor gain model (two-point linear fit): gain_dB ≈ 0.67 × raw + 19.3 (±1 dB residual at second point).

### FX reverb type (BULK_02) — vocabulary resolution
- kSpace → displays **"Basin"** in Serum 2.0.23
- Basin type uses different parameter labels than Hall:
  - 3rd knob: **FEEDBACK** (not DECAY — reverb type-dependent label)
  - Spin section: **CHORUS** label (not SPIN)
- LO CUT=0, HI CUT=35, CHORUS RATE=25, DEPTH=20 (defaults, same as Hall)
- This resolves the schema vocabulary gap: serum-mcp calls it 'kSpace', Serum displays 'Basin'

Reverb type vocabulary (all confirmed in Serum 2.0.23):
- kHall → **'Hall'** (BULK_01)
- kSpace → **'Basin'** (BULK_02; schema calls it 'kSpace', display is 'Basin')
- kAbyss → **'Nitrous'** (VERIFY_REFERENCE_FULL, CORRECTION 2)
- kVintage → untested (likely 'Vintage')
- Serum menu order: Plate / Hall / Vintage / Nitrous / Basin (5 options)
- Schema has: kAbyss/kHall/kSpace/kVintage (4); 'Plate' has no schema word

### LFO shape vocabulary (BULK_02, direct label read)
- LFO3 (file LFO2, kParamType='RandomSH'): shows **'S&H'** — schema word='RandomSH', display='S&H' ✓
- LFO4 (file LFO3, kParamType='Rossler'): shows **'Chaos: Rossler'** — LABEL MATCH (schema word IS 'Rossler'; Serum prefixes the category 'Chaos:' in the dropdown, same as 'S&H' from 'RandomSH')
- LFO5 (file LFO4, kParamType='Lorenz'): shows **'Chaos: Lorenz'** — LABEL MATCH (same category prefix pattern)
- LFO6 (file LFO5, kParamType='Path'): shows **'Path'** — MATCH ✓
- LFO mode: all BULK_02 LFOs have mode=Free (different from BULK_01's Envelope)

Note on LFO shape display vocabulary: schema raw string is the shape identifier (e.g. 'Rossler'); Serum prepends a category label ('Chaos:') in the UI dropdown for the chaotic shapes. The raw file string remains the unadorned word.

### OSC SAMPLE mode (BULK_02, file-confirmed)
- OSC A/B/C type: kOsc_Sample (SAMPLE mode confirmed from file + MIX page shows only COMPRESSOR and REVERB in FX list)
- Loop crossfade values from host text (B_host_text, BULK_02_host_texts.json):
  - A Loop X-Fade: raw=0.83 → '83' ✓
  - B Loop X-Fade: raw=0.56 → '56' ✓
  - C Loop X-Fade: raw=0.30 → '30' ✓

## Not covered on this pass
filter1/2 numeric knobs, macros 2-4/6-8, master volume (no tooltip), global note_latch /
limit_same_note_polyphony / fx_bus destinations, bode/chorus/convolve/delay params beyond the earlier scan.

## Note on the live instance
To test the LFO constraint, LFO1 on the loaded Ableton track was changed to shape Normal + mode Envelope. The
.SerumPreset file on disk was not modified. Reload the preset before any further screen reads of LFO1.

## mixer (MIX page), hover-confirmed
- sub bus1 50%, filter2 bus2 50%, sub pan -25 L, noise pan -25 L, filter1 wet 50%, filter2 wet 50% MATCH
- `mixer.*.filter_balance`: control not located on the MIX page (the icon under each strip is the Env1 routing toggle)

## Confirmed on screen (OSC page)
- oscA disabled (enabled=0) MATCH; oscA/B/C OCT -2, SEM -6, FIN -40, UNISON 9, LS 50, LE 50 MATCH
- noise type Pink MATCH; filter1 type Low 12 MATCH
- env1: 5.00 s / 2.60 s / 16.0 s / -12.0 dB / 16.0 s (sustain = known finding)
- lfo1 rate 500.0 Hz (rate 50 x10, Hz mode) MATCH; mono, triplets, dotted lit MATCH
- voicing MONO + LEGATO on, POLY 17 MATCH; bend +12/-12 MATCH; transpose -24, swing 25.0% MATCH; porta ALWAYS + SCALED MATCH
