# Bulk DIRECT_UI verification: optimization report

Generated by `build_bulk_ui_verification.py` from repository evidence only. Nothing was loaded into Serum, rendered or verified.
Page/tab structure is INFERRED from Serum's module layout and the scan notes, because no layout screenshots were available;
the preset count does not depend on it, only the pass estimate does.

## 1. Accounting: 330 controls, each exactly once

| layer | counts |
|---|---|
| A. causal (closure_ledger_v3 status) | STATE_QUALIFIED_UI_PENDING 312, STATE_NOT_OBSERVED 9, CLOSED 6, NOT_DERIVED 2, PROMOTION_BLOCKED 1 |
| B. host text (campaign) | OBSERVED 188, UNOBSERVABLE 140, NOT_ATTEMPTED 2 |
| C. DIRECT_UI (direct_ui_evidence_v2) | UI_NOT_SCANNED 240, UI_CONFIRMED 60, UI_NOT_OBSERVABLE_IN_CONTEXT 12, UI_MISMATCH 7, UI_CONTEXT_CONFLICT 6, UI_VOCABULARY_DISCOVERED 1, UI_CURVE_UNVERIFIED 1, UI_PLAUSIBLE 1, UI_VOCABULARY_MISMATCH 1, UI_OBSERVED_NO_TARGET 1 |
| C. DIRECT_UI supplementary (session 2, recorded separately) | 12 controls (warp on the WT secondary preset) |
| host identity crosswalk | HIGH 166, NONE 137, LOW 15, MEDIUM 12 |
| host-mapped (HIGH/MEDIUM) but not GUI-confirmed | 138 |
| alias pairs (one raw path, verified once) | 9: mixer.filter1.enable=filter1.enabled, mixer.filter1.wet=filter1.wet, mixer.filter2.enable=filter2.enabled, mixer.filter2.wet=filter2.wet, oscNoise.pan=mixer.noise.pan, voice.voicing.legato=global.legato, voice.voicing.mono=global.mono, voice.voicing.porta_always=global.porta_always, voice.voicing.porta_scaled=global.porta_scaled |

Closure after this build: PENDING_BULK_UI_READ 267, UI_CONFIRMED 60, RESIDUAL_NOT_DERIVED 2, RESIDUAL_NOT_STORED 1.

## 2. Genuinely unresolved -> new workload

- Already DIRECT_UI-confirmed and not an anchor: **60** (subtracted).
- Verified through an alias partner: **9**.
- Cannot be put in any preset (residual): **3**: `arp.transpose.shape` (NOT_DERIVED: raw stored vocabulary unknown, so no valid raw value can be written); `global.use_ultra_on_render` (Serum never stored any written value for this key in the campaign (STATE_NOT_OBSERVED): no preset can carry it); `global.voice_priority` (NOT_DERIVED: raw stored vocabulary unknown, so no valid raw value can be written).
- Needs a bulk read: **258** controls (representatives), anchors included deliberately.

## 3. The batching problem and its solution

Conflict graph over the 258 work controls. The ONLY hard edges the evidence establishes:

1. **Oscillator engine mode** (18 edges): WTOsc controls (warp_*, wavetable) need WAVETABLE and SampleOsc controls need SAMPLE on the same slot.
2. **LFO shape/mode coupling** (4 edges): mode=Envelope is disabled under S&H (proven twice on screen). Whether chaotic shapes (Rossler/Lorenz/Path) also disable Envelope is unknown, so a shape test and a mode=Envelope test never share an LFO.

Everything else (all 12 FX units in one rack, 4 envelopes, 6 LFOs, both filters, mixer, ARP, global, voicing, macros) has no edge: the giant preset already proved the 12-unit rack and all sections load together.

**Result: 2 presets. Lower bound: 2** (the graph has an edge, so one preset is impossible). The partition is asserted to be a proper colouring, so **2 is optimal**.

| preset | controls (+aliases) | osc engine | FX rack | structural reason |
|---|---|---|---|---|
| `BULK_01_WAVETABLE_MAIN` | 251 (+9) | WAVETABLE | FXBode, FXChorus, FXComp, FXConv, FXDelay, FXHyperD, FXDistortion, FXEQ, FXFilter, FXFlanger, FXPhaser, FXReverb | all WAVETABLE controls + every LFO mode=Envelope test under the default shape + every edge-free control; carries the LFO 7 conflict reference |
| `BULK_02_SAMPLE_CALIBRATION` | 12 (+0) | SAMPLE | FXComp, FXReverb | the SAMPLE-mode controls (A/B/C loop crossfade) + LFO shape tests (split from mode tests) + second calibration points riding the mandatory second load |

## 4. Fingerprints (how each value was chosen)

- Continuous: golden-ratio sequence over the safe interior 15-85% of the range (log domains in log space), rounded to display steps. One counter runs across the whole preset, so sibling controls (env1-4, lfo1-6, osc A/B/C) never share a value; the validation checks that per view and per field family.
- The safe range is shrunk where the campaign's host texts show Serum saturating (for example `global.transpose` shows -24 for -48 and -24, and `bend_range_up` stays 24 above 24). It is also shrunk where the screen already showed a word instead of a number (compressor ratio 'Limit' at 31622, so the new points are 430 and 210).
- Integer rounding is used only when the display equals the raw value (octave, semitone). Scaled integer displays such as warp '25' for 0.25 keep fine raw steps.
- Enums rotate through non-default words per vocabulary across the whole preset (for example warp mode A/B/C = sync/PWM/bend; reverb Hall in BULK_01 and Space in BULK_02, where Space is one of the schema words missing from Serum). Booleans flip. Macro names are self-identifying text.
- **Uniqueness rule**: every numeric raw value is unique across the WHOLE preset. Every expected on-screen number (the raw value, the host-text model prediction, and the percentage for 0-1 controls) is unique within its page. Enum labels are not repeated on a page while the vocabulary has another option. A strict all-pages display uniqueness is impossible: more than 99 controls display a 0-100 integer or percentage, and only 99 whole numbers fit strictly inside that range.
- Shared by necessity in `BULK_01_WAVETABLE_MAIN` (identified by control identity and position, not value): "Envelope" x6 (lfo1.mode, lfo2.mode, lfo3.mode, lfo4.mode, lfo5.mode, lfo6.mode); 0.0 x3 (arp.playback.offset, global.voice_amp, oscA.enabled); 1.0 x54 (booleans); 10.0 x2 (arp.playback.repeats, global.voice_control.scaling.lfos); 2.0 x3 (fx.delay.mode, global.fx_bus2_destination, global.oversampling).
  Open-range controls can only use values Serum itself stored, so a few coincide across pages. The six LFO modes are 'Envelope' by design, because that is the test.
- `predicted_display` in the manifests is a B-tier model fitted to the campaign host texts. It is a hypothesis for the screen read to confirm or refute, never a verdict.

## 5. Calibration anchors (deliberately placed)

| control | preset | value | predicted display (B-tier hypothesis) |
|---|---|---|---|
| `env1.sustain` | BULK_01_WAVETABLE_MAIN | 0.16 | -31.84 (db_square) |
| `env2.sustain` | BULK_01_WAVETABLE_MAIN | 0.55 | 30.25 (percent_square) |
| `env3.sustain` | BULK_01_WAVETABLE_MAIN | 0.56 | 31.36 (percent_square) |
| `env4.sustain` | BULK_01_WAVETABLE_MAIN | 0.45 | 20.25 (percent_square) |
| `fx.compressor.attack` | BULK_01_WAVETABLE_MAIN | 22.0 | - |
| `fx.compressor.attack` | BULK_02_SAMPLE_CALIBRATION | 10.0 | - |
| `fx.compressor.gain` | BULK_01_WAVETABLE_MAIN | 11.0 | - |
| `fx.compressor.gain` | BULK_02_SAMPLE_CALIBRATION | 8.0 | - |
| `fx.compressor.ratio` | BULK_01_WAVETABLE_MAIN | 430.0 | - |
| `fx.compressor.ratio` | BULK_02_SAMPLE_CALIBRATION | 210.0 | - |
| `fx.compressor.thresh` | BULK_01_WAVETABLE_MAIN | 0.49 | - |
| `fx.compressor.thresh` | BULK_02_SAMPLE_CALIBRATION | 0.4 | - |
| `fx.reverb.type` | BULK_01_WAVETABLE_MAIN | 'kHall' | - |
| `fx.reverb.type` | BULK_02_SAMPLE_CALIBRATION | 'kSpace' | - |
| `lfo1.mode` | BULK_01_WAVETABLE_MAIN | 'Envelope' | - |
| `lfo2.mode` | BULK_01_WAVETABLE_MAIN | 'Envelope' | - |
| `lfo3.mode` | BULK_01_WAVETABLE_MAIN | 'Envelope' | - |
| `lfo4.mode` | BULK_01_WAVETABLE_MAIN | 'Envelope' | - |
| `lfo5.mode` | BULK_01_WAVETABLE_MAIN | 'Envelope' | - |
| `lfo6.mode` | BULK_01_WAVETABLE_MAIN | 'Envelope' | - |
| `oscA.warp_amount2` | BULK_01_WAVETABLE_MAIN | 0.63 | 63.0 (percent_linear) |
| `oscA.warp_amount` | BULK_01_WAVETABLE_MAIN | 0.46 | 46.0 (percent_linear) |
| `oscA.warp_mode2` | BULK_01_WAVETABLE_MAIN | 'am' | - |
| `oscA.warp_mode` | BULK_01_WAVETABLE_MAIN | 'kSync' | - |
| `oscA.warp_var2` | BULK_01_WAVETABLE_MAIN | 0.79 | 0.79 (identity) |
| `oscB.warp_amount2` | BULK_01_WAVETABLE_MAIN | 0.32 | 32.0 (percent_linear) |
| `oscB.warp_amount` | BULK_01_WAVETABLE_MAIN | 0.42 | 42.0 (percent_linear) |
| `oscB.warp_mode2` | BULK_01_WAVETABLE_MAIN | 'sync' | - |
| `oscB.warp_mode` | BULK_01_WAVETABLE_MAIN | 'kPWM' | - |
| `oscB.warp_var2` | BULK_01_WAVETABLE_MAIN | 0.22 | 0.22 (identity) |
| `oscC.warp_amount2` | BULK_01_WAVETABLE_MAIN | 0.61 | 61.0 (percent_linear) |
| `oscC.warp_amount` | BULK_01_WAVETABLE_MAIN | 0.18 | 18.0 (percent_linear) |
| `oscC.warp_mode2` | BULK_01_WAVETABLE_MAIN | 'pwm' | - |
| `oscC.warp_mode` | BULK_01_WAVETABLE_MAIN | 'kBendPos' | - |
| `oscC.warp_var2` | BULK_01_WAVETABLE_MAIN | 0.35 | 0.35 (identity) |

- **Sustain** (host texts, B tier): Env 1 shows -24.1/-12.0/-5.0 dB and Env 2-4 show 6/25/56 % for raw 0.25/0.5/0.75. That is exactly display = raw², so the 'mismatch' is very likely a squared display curve, not a bad write. BULK_01's four sustains (0.54/0.17/0.24/0.57) predict -10.7 dB / 3 % / 6 % / 32 %: reading them on screen confirms or refutes the model in one pass.
- **LFO**: in BULK_01, LFO1-6 have mode=Envelope with the default shape (the compatible proof). LFO 7, which is outside the 330, has S&H + Envelope as the native-conflict reference, so the conflict and the compatible case are seen side by side in one load.
- **Warp**: BULK_01 keeps A/B/C in WAVETABLE with distinct warp modes and amounts. `warp_mode2` is written through its leaf set (raw `kFM_OSC`-style values, not the spec word `'fm'` the giant builder wrote), and then `warp_amount2` is written, because the leaf set resets `kParamWarp2`.

## 6. Local screen work

**BULK_01_WAVETABLE_MAIN**: 18 page/tab views, about 168 numeric reads (hover unless shown directly).

- ARP x1: ARP
- BOTTOM x1: MACROS
- FX x2: FX_bode, FX_chorus, FX_compressor, FX_convolve, FX_delay, FX_dimension, FX_distortion, FX_filter, FX_flanger, FX_hyper, FX_phaser, FX_reverb
- GLOBAL x1: GLOBAL
- MIX x1: MIX
- OSC x12: ENV1, ENV2, ENV3, ENV4, FILTER1, FILTER2, LFO1, LFO2, LFO3, LFO4, LFO5, LFO6, LFO7, NOISE, OSC_A, OSC_B, OSC_C

**BULK_02_SAMPLE_CALIBRATION**: 6 page/tab views, about 7 numeric reads (hover unless shown directly).

- FX x1: FX_compressor, FX_reverb
- OSC x5: LFO3, LFO4, LFO5, LFO6, OSC_A, OSC_B, OSC_C

Readability limits known from the scans: UNKNOWN_OR_READABLE 243, NO_TOOLTIP 19, NOT_LOCATED 5. Filter numeric knobs are the biggest block, and they need the filter's expanded view to be found; the `mixer.*.filter_balance` controls need locating first.

## 7. Residual local workload (exact)

1. Load `BULK_01_WAVETABLE_MAIN` and read its views; load `BULK_02_SAMPLE_CALIBRATION` and read its views: **2 loads, 24 views, about 175 numeric reads**.
2. `arp.transpose.shape`: 18 display labels are known; the raw strings are not. Set each label in the GUI, save, and read the file back: **18 save/readbacks** (or fewer if the raw strings turn out to equal `arp.pattern.shape`'s, which one save of 'Up' would indicate).
3. `global.voice_priority`: enumerate its menu labels, then one save/readback per label.
4. `global.use_ultra_on_render`: toggle it in the GUI, save, and read the file (1 op). Serum never stored a written value, so a preset cannot set it.
5. Locate the filter-balance control on screen for `mixer.noise.filter_balance`, `mixer.osc_a.filter_balance`, `mixer.osc_b.filter_balance`, `mixer.osc_c.filter_balance`, `mixer.sub.filter_balance` (5 pending controls), 1 search.
6. Reserve only: the 1,035 isolation presets and the per-control BISECT files, for a control whose bulk reading is ambiguous.

## 8. Why this is close to the minimum

- **Loads**: 2 is the proven lower bound. Each oscillator slot must be seen in WAVETABLE and in SAMPLE mode, and a slot has one mode per preset. Any 1-preset plan leaves either 3 SAMPLE or 18 WAVETABLE controls unverifiable.
- **Views**: every view in BULK_02 is forced by a control that cannot be in BULK_01: the SAMPLE osc view, the LFO shape tabs and the FX view (FX only because the second compressor/reverb point rides a view that the second load already needs; dropping it saves 1 view but loses the second curve point for 4 anchors). BULK_01's views are one per UI region that holds unverified controls; no region is visited twice.
- **Compared with the isolation corpus**: 1,035 loads, each followed by at least 1 view, versus 2 loads and 24 views: **518x fewer loads** and about 43x fewer views.
- **What stays unminimised**: page passes assume the inferred layout. With the layout screenshots the pass count can be tightened (for example if ENV/LFO tabs share a view), but the preset count cannot go below 2.

## 9. Validation (run by the builder; any failure aborts the build)

- `BULK_01_WAVETABLE_MAIN`: repacked file == intended body (roundtrip), 0 unintended leaves vs its structural base, every intended leaf landed (last writer wins, superseded side leaves recorded), numeric raw values globally unique, displays unique per page, 0 fingerprint collisions (view / sibling family / baseline / rider).
- `BULK_02_SAMPLE_CALIBRATION`: repacked file == intended body (roundtrip), 0 unintended leaves vs its structural base, every intended leaf landed (last writer wins, superseded side leaves recorded), numeric raw values globally unique, displays unique per page, 0 fingerprint collisions (view / sibling family / baseline / rider).
- Every value lies inside its declared domain (and inside Serum's observed effective range where known); enum values are vocabulary members; leaf-set words come from the manifest table.
- 330 entries in `serum_full_control_map_v1.json`, unique. Existing evidence files are read-only inputs: only new files are written.
- Tiers stay separate: `B_host_text.is_direct_ui` is false everywhere, and session-2 readings live in `C_direct_ui.session2_supplementary`, not merged into `direct_ui_evidence_v2`.
