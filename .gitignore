__pycache__/
*.pyc
*.pyo

# Secrets — never commit
.env
.env.*

# Large generated/cache artifacts — never commit these
experiments/_corpus_cache.pkl
*.corpus_cache.pkl

# Runtime-generated production data (renders, episodes, experiences, memory)
serum2/data/

# Qwen VLM experiment (large model weights, separate venv)
models/
.venv_qwen/
